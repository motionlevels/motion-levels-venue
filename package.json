{
  "name": "motion-levels-venue",
  "private": true,
  "scripts": {
    "test": "npm run test:static && npm run test:python",
    "test:static": "node --test test/static/*.test.cjs",
    "test:python": "python3 -m unittest discover -s deploy/motionlevels-pc/tests"
  }
}

{
  "scripts": {
    "test": "npm run test:static && npm --prefix apps/player-menu test",
    "test:static": "node --test test/static/*.test.cjs",
    "check:player-menu": "npm --prefix apps/player-menu test && npm --prefix apps/player-menu run build",
    "check:player-display": "npm --prefix apps/player-display run build",
    "verify:games-bundle": "node scripts/verify-motion-levels-games-bundle.mjs"
  }
}
